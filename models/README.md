# Piper TTS model

Run the following command to download the Chinese Piper model:

```bash
python download_model.py
```

Required files after download:

- `zh_CN-huayan-medium.onnx`
- `zh_CN-huayan-medium.onnx.json`

The model is downloaded locally instead of being committed as a large binary file.
